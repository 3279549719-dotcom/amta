你是一个只读的分支审计员。请对下面 4 个本地分支逐个核查，然后一次性通过 AskUserQuestion 向我提出 4 个问题（每个分支一个），让我逐条拍板。

【已有背景事实（你可再用只读 git 命令复核）】
- main 当前在 7c39ffd，origin/main 在 5477d9，main 领先 origin/main 约 75 个提交，且两者历史已分叉（origin 含 relocate 提交）。
- 待处置的 4 个本地分支：
  1. chore/047c548-cleanup —— 1 个提交（09-04），清理遗留引用/归档旧测试/OCR 收敛，merge-base 为 d2869f3
  2. feat/terminology-pre-scan-impl —— 16 个提交，术语预扫描实现，merge-base 为 f858945；注意 main 在 09-03 已合入过 feat/terminology-pre-scan，疑似内容重复
  3. refactor/modular-architecture —— 6 个提交（08-30），ADR-024 模块边界重构，merge-base 为 b31caf8
  4. refactor/orchestrator-report-convergence —— 6 个提交，报告收敛 + run_pipeline 单入口 + e2e 测试，merge-base 为 5477d9；main 在 09-05 已做过类似收敛，疑似过时
- 另有一个 stash@{0}（WIP on feat/terminology-pre-scan: ee77170），不在此次问题范围内。

【执行步骤】
1. 对每个分支，运行只读命令确认它相对 main 的真实差异（例如 git log --oneline main..<branch>、git merge-base main <branch>、git diff --stat main...<branch>），核实"是否已被 main 吸收/重复/仍有独有提交"。
2. 基于核查结果，对每个分支构造一个问题，问题是"这个分支该怎么处置？"，header 用分支名，选项给出你基于差异判断的具体建议（3 个左右，例如"内容已被 main 吸收，建议直接删除""仍有独有提交，建议合并入 main 后删除""暂缓保留，先不动"），每个选项 description 写清你判断的依据（如独有提交数、重复程度）。
3. 一次 AskUserQuestion 调用传全部 4 个问题（multiSelect 均为 false）。
4. 拿到我的回答后，用不超过 5 行总结你核实到的差异和你最终建议，然后结束。

【硬约束】
- 全程只读：不得修改/删除任何文件、不得 checkout/merge/delete 任何分支、不得改 git 配置。只允许 git log / git diff / git branch / git merge-base / git rev-parse / git status 这类只读命令。
- 不要做任何写文件操作。
