你是代码仓库维护助手。用户已经明确拍板：删除以下 4 个本地分支（审计结论为内容已被 main 吸收或用户明确判删）：

1. chore/047c548-cleanup
2. feat/terminology-pre-scan-impl
3. refactor/modular-architecture
4. refactor/orchestrator-report-convergence

【执行要求】
1. 用 git branch -d 逐个删除（若因未合并失败，改用 git branch -D 强制删除——宿主已授权两种方式）。
2. 删除前先运行 git branch 列出确认这 4 个分支存在；删除后再次 git branch 确认 4 个分支已消失、main 不受影响。
3. 全程不要碰其他分支、不要动 main、不要 push、不要改任何文件内容。
4. 完成后用 3 行以内总结：删了哪几个、最终本地分支列表、main 状态。
