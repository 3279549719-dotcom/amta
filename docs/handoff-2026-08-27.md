# Handoff 2026-08-27 —— 会话交接文档

> 给下一个 AI 会话（DSH / Claude Code / 任何 agent）的完整交接。先读本文件 + `docs/progress.md`（当前状态唯一事实源）。
> 本会话（2026-08-27，OpenClaw eirin 侧）完成：契约甄别 → Phase 1 契约卫生 → 11-20 全量重跑 → Stage 4 inpaint → Stage 5 typeset → 冒烟 → /finish 推送。

---

## 0. 仓库状态（最关键）

- **分支**：`feat/contract-hygiene`（HEAD = 322a11b，**已推送** origin，28 commits）
  - 内容：detect 修复（feat/detect-union-4d 并入）+ Phase 1 契约（ADR-019）+ Stage 4（ADR-020）+ Stage 5（ADR-021）+ /finish 落盘
- **未合并 main、未建 PR**。合并前建议先完成渲染质量修复（见 §4 第 0 项）
- 工作区：`workspace/touhou-single-wing/`（artifacts 已 gitignore 不入库；state JSON 已入库）
- 旧产物备份：`output/backup/2026-08-27-pre-rerun-11-20/`（54 文件）
- worktree `.worktrees/feat-stage5-typeset` 已移除；SDD 台账 `.superpowers/` 已删（已 gitignore）

## 1. 本次完成事项

### 1.1 契约甄别（决策源头）
- 对 Gemini Stage 4-6 蓝图 12 个契约点逐项甄别（Backward Reasoning），结论：**5 落地**（category 三级分类 / sub_tier 透传 / region_id 统一 u00 / items 改名 / image_meta）+ **2 延后**（sfx_triage、polygon）+ **5 砍掉**（reading_direction、candidates、speaker、char_count、state_updates）
- Spec 入库：`docs/superpowers/plans/2026-08-27-stage46-guidance-spec.md`（Patrick + 外部顾问最终决策）
- 关键依据：业界标杆 manga-image-translator 无磁盘契约（内存对象传递）；磁盘契约=最小充分

### 1.2 Phase 1 契约卫生（ADR-019）
- `geometry.assign_category`：bubble_type→category（dialogue/narration/unknown→dialogue_bubble 保守，sfx→sfx，overlay_text 透传）
- 02_ocr：canon 透传 category/sub_tier；返回 doc 字段 regions→items
- canon_schema：可选字段枚举校验（向后兼容）
- 01_detect：image_meta（width/height/channels）

### 1.3 11-20 全量重跑（run 043db62d2c5c）
- 新 4-detector 并集 + 新契约 + deepseek-v4-flash，10 页零失败
- **canon 108 条**（手工真值 97，宁多勿漏 +11），页 15 差 1 框（唯一未达标）
- category 覆盖 108/108；sub_tier 41 条
- 语义评审：8/10 页 pass_rate 1.0；页 14 0.889（auto-repair 处理）；页 13 有 **2 条 inconclusive 待导演复核**（page_13_u03、page_13_u09，评分 null）
- 翻译对照报告：`output/reports/translation_check_11_20.html` + zip（已发邮箱）

### 1.4 Stage 4 inpaint（ADR-020 + 探针定案）
- 探针结论（重要契约）：
  - mask role = **segment + bubble 双 mask**，payload 必须 **PNG 编码字节**（裸像素 400）
  - `run_pipeline([page_id], ["lama-manga"])` ~20s（400×600 CPU）；整页 2243×3465 预计 1-3min
  - 结果取回：**弃 export**（400/422）→ `get_page_nodes` 找 `kind.image.role=="inpainted"` → `get_blob` → **WEBP**
  - 必须先 `close_current_project` + `create_project` 再 `import_page`（400 教训）
- 新增：`src/amta/inpaint_strategy.py`（fill_white/inpaint/skip 策略）、`KoharuClient.run_inpaint/fetch_inpainted`、`scripts/04_inpaint.py`（clean 产物 + pixel_diff_ratio 机械检查）
- 范围外：sfx_triage 判定（side_annotation/skip）延后；overlay 检测盲区未补

### 1.5 Stage 5 typeset（ADR-021，自研 Pillow 引擎）
- 新增：`src/amta/fonts.py`（4 级字体映射：dialogue→msyh / overlay·narration→simkai+2.5px 描边 / 呼喊→msyhbd / sfx→FZSTK；降级链）、`src/amta/typeset_engine.py`（decide_direction **含 overlay 强制竖排漏洞修复** / wrap_text 避头尾 / fit_font_size 二分 12-52）、`src/amta/typeset_render.py`（横排居中/竖排单列/白描边）、`scripts/05_typeset.py`（coverage/overflow 检查）
- bbox 关联：`canon.node_id → detection.blocks[].node_id`（零契约改动）
- 00_run_all：新增 `--with-inpaint` / `--with-typeset`（04/05 在语义评审+修复后执行）
- 产物：`*_typeset.json` + `final/<page>_final.png`

### 1.6 冒烟验证（页 11）
- 04：filled=9（全气泡走白底直填 bypass）、pixel_diff_ratio=0.0899
- 05：rendered 9/9、coverage_complete=true、竖排 2 条（u06 52px/u07 27px）、感叹→粗体映射、字号 20-52 自适应
- `final/page_10_final.png` 已生成（**渲染观感有问题，见 §4**）

### 1.7 /finish 落盘
- progress.md 更新；lessons **L28-L31**（PS 管道缓冲假象 / ConvertFrom-Json 坑 / CRLF 编辑 / koharu mask 契约）
- CLAUDE.md 工具面补 Stage 4-6 一行；state 三件套入库；ADR-019/020/021 已建
- **fastcheck 192 全绿**（+28 测试）

## 2. 环境速查

- Python 3.13（唯一带 pytest/ruff/pyright 的）：`C:\Users\asus\AppData\Local\Programs\Python\Python313\python.exe`；提交前 PATH 前置它
- 引擎：koharu v0.59.1 REST :4000（钉版）；llama-server :8118（OCR）；DeepSeek `.env`（CHAT_MODEL=**deepseek-v4-flash**，flash-vision 评审）
- 源图：`D:\我的汉化\汉化作品\东方\单翼停留之地\N.jpg`（41 页）
- 工作集：`workspace/touhou-single-wing/`；01→02→03→[04→05]→[评审→修复] 顺序（00_run_all）
- 邮件：1736683492@qq.com；send-email 技能注入失效，用包装器 `C:\Users\asus\.openclaw-autoclaw\workspace\tmp_send_email_wrapper.py`（从 openclaw.json 读 SMTP，不打印密钥）
- 中文多行 JSON 用 Python 解析（PS ConvertFrom-Json 会失败，L29）

## 3. 下一会话建议入口

1. **修渲染质量（Patrick 已指出问题）**：字号跨框不一致 + 部分框内译文绘制/擦除观感问题。建议：字号按档位化（12/16/20/24/28/36/48/52）而非连续二分；校验译文落在 bbox 中心、不重叠；overlay 竖排锚点微调
2. **11-20 全量 `--with-inpaint --with-typeset`**（~30-50min）：验证 sfx/overlay 真实擦除路径，产出全彩成品；先删 page_1x 的 inpaint/typeset/final 产物
3. **页 13 两条 inconclusive 复核**（page_13_u03/u09，导演终审）
4. **Stage 6 QA 工位**（coverage 反压闭环 + VLM 终审，蓝图 Phase 4）
5. **合并 main + PR**（feat/contract-hygiene → main；push 铁律：fetch→rebase→push，禁 force-push）
6. 工单微信通知（handoff 遗留项）
7. 41 页全书跑批（预计 OCR/翻译 ~6h+，lama 擦除另计）

## 4. 已知问题 / 风险

- ⚠️ 渲染观感：字号跨框不一致、译文绘制位置/擦除视觉问题（Patrick 初审否决，最高优先）
- ⚠️ 页 15 检测差 1 框（唯一未达真值页）
- 页 13 2 条 inconclusive 未终审
- lama-manga 整页擦除慢（1-3min/页），41 页约 1-2h；可考虑局部 patch 优化（蓝图 Anime-LaMa 裁剪思路，未做）
- overlay_text 检测盲区（服装字等框外字漏检）未补强
- sfx side_annotation 旁注布局未实现（等 sfx_triage）
- 多列竖排/任意多边形内切未实现（MVP 单列竖排）

## 5. 本会话执行要点复盘（给下一个 agent 的提示）

- 后台长任务验证以**产物文件为准**（Tee 缓冲会骗人，L28）
- 编辑 CRLF 文件用 Python 脚本替换（edit 工具匹配不上，L30）
- subagent 基建（sessions_spawn）当日两次 LLM 故障 → 可先试 inline 执行或换模型重试
- 测试桥惯例：`scripts/_NN_name.py`（数字前缀 import 限制）；monkeypatch 需加载**真实脚本**而非桥
- 所有计划/规范在 `docs/superpowers/plans/`（stage4/stage5 计划 + spec + 探针 findings）
