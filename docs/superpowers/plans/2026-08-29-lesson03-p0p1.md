# Lesson 03 P0+P1 执行计划（front3 合流 + 测试补齐 + Slice 2）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Lesson 02 线（feat/front3-reconstruction）合流进 Lesson 03 线（feature/context-semantic-transfer），补齐 get_context 的单元测试缺口并把集成 A/B 测试收编进 pytest，完成 Stage 2-d Slice 2（CLAUDE.md 记忆库触发行 + 写回约定）。

**Architecture:** 本计划不改任何业务架构——Task 1 是纯 git 合流 + 单文件冲突手工合流（保留 front3 的 lookup_image/VISION_BUDGET 与 feature 的语义化 get_context 两族代码）；Task 2 在合流后的代码上补行为锁定测试与一个 TDD 小修复；Task 3 在独立 worktree（feat/project-memory）上做文档层改动。

**Tech Stack:** Python 3.12 + pytest（主检出 .venv）；git（无框架）；Windows + PowerShell（文件写操作一律走 Python utf-8）。

**Spec:** `E:\Agent Loop Learning\lessons\lesson03-handover-review.html`（交接审查报告，Patrick 于 2026-08-29 批准做 P0+P1）；补充权威：`E:\Agent Loop Learning\STATE.md` 待办 #8/#9（Slice 2 规格）。计划与 spec 冲突时以 spec 为准。

## Global Constraints

- main 分支不动；全程不 push；不 force-push。
- 修改文件一律用 Python `Path.write_text(encoding="utf-8")`；禁止 PowerShell `Set-Content`/`>` 重定向写文件（仓库已知编码坑）。
- 测试命令固定为：`E:\manga translator agent\amta\.venv\Scripts\python.exe -m pytest tests\ -q`（主检出 venv 有 pytest；worktree 的 venv 没有，不得使用）。
- `git add` 只用显式路径；禁止 `git add -A` / `git add .`（`.superpowers/`、`workspace/` 产物等不得卷进提交）。
- 不运行任何需要 API key 的 LLM/VLM 调用（不跑 `run_real_data_test.py` 等真实翻译脚本）。
- worktree 内已检出的分支不得切换（不对 `.worktrees\*` 执行 checkout/switch）；合并操作只在主检出做。
- commit message 遵循仓库 `<type>: <中文描述>` 风格（feat/fix/test/refactor/docs/merge）。
- 所有子任务报告写到 SDD workspace：`E:\manga translator agent\amta\.superpowers\sdd\2026-08-29-lesson03-p0p1\`（已 gitignore）。

---

### Task 1: 合流 feat/front3-reconstruction 进 feature/context-semantic-transfer

**Files:**
- Modify: `src/amta/translate_tools.py`（预期唯一冲突文件，手工合流）
- 自动合并：`src/amta/translate.py`、`src/amta/vlm_verify.py`、`src/amta/stage3_planner*.py`、`tests/test_stage3_planner*.py`、`tests/test_lookup_image.py`、`tests/test_front3_*.py`、`scripts/eval_*.py` 等（front3 单侧变更）

**Interfaces:**
- Consumes: 两个分支当前状态——feature/context-semantic-transfer @ 92438f2（主检出，含语义化 get_context）；feat/front3-reconstruction @ 1c856b6（含 lookup_image + Stage3 方案A/B）；merge-base = db633a8。
- Produces: 合流后的 `translate_tools.py` 同时具备 lookup_image（front3）与 `_build_semantic_context` 函数族（feature）；全库 pytest 全绿；后续 Task 2 在此代码上写测试。

- [ ] **Step 1: 预检 + 备份指针**

```powershell
git -C "E:\manga translator agent\amta" status --short        # 必须为空
git -C "E:\manga translator agent\amta" rev-parse HEAD        # 记录 BASE（应为 92438f2）
git -C "E:\manga translator agent\amta" branch backup/2026-08-29-pre-front3-merge
git -C "E:\manga translator agent\amta\.worktrees\feat-front3-reconstruction" status --short   # 必须为空
```

- [ ] **Step 2: 发起合并**

```powershell
git -C "E:\manga translator agent\amta" merge --no-ff feat/front3-reconstruction -m "merge: 合流 front3 线（Lesson 02 lookup_image + Stage3 方案A/B）进 Lesson 03 线"
```

预期：`src/amta/translate_tools.py` 冲突（两线都改过它，分歧 331 行）；其余文件自动合并。若出现其他冲突文件，按"保留两侧意图的并集"处理并在报告中逐个说明。

- [ ] **Step 3: 手工合流 translate_tools.py**

目标形态 = **front3 版本为基底**（保留其 lookup_image 工具声明 + execute_tool 分支 + run_tool_loop 透传 + lookup_term 等全部内容），**再移植 feature 侧的 get_context 家族**：

1. 移植常量块：`CATEGORY_LABELS`、`MAX_REGIONS_PER_PAGE = 15`、`MAX_RELATIONSHIPS = 3`、`MAX_TERMS = 5`。
2. 移植 5 个函数（以 feature @ 92438f2 版本为准，全文照搬）：`_build_semantic_context`、`_read_page_blocks_from_artifacts`、`_read_fallback_context`、`_format_relationships`、`_format_relevant_terms`。
3. `execute_tool` 的 `get_context` 分支改为：`pages = max(1, min(int(args.get("pages") or 3), 3))` 然后 `return _build_semantic_context(pages, work_state or {}, prev_pages, state_dir)`（保持 front3 版 execute_tool 的函数签名与 prev_pages 参数不动）。
4. 删除 front3 版本里的旧 get_context 渲染逻辑（被上面替换），不得残留死代码。
5. 检查 imports 并集（feature 版 get_context 家族用到 `re`、`json`、`norm` 等，front3 基底可能已有）。

用 Python 脚本或逐段编辑完成移植（utf-8），不用 PowerShell 写文件。

- [ ] **Step 4: 完成合并提交**

```powershell
git -C "E:\manga translator agent\amta" add src/amta/translate_tools.py
git -C "E:\manga translator agent\amta" commit --no-edit   # 或补充说明后完成 merge commit
```

- [ ] **Step 5: 全量验证**

```powershell
E:\manga translator agent\amta\.venv\Scripts\python.exe -m pytest tests\ -q
```

预期：全绿（原 209 个 + front3 线 test_stage3_planner 16 + test_stage3_planner_vision 9 + test_lookup_image 18 + test_front3_* 若干，合计约 250+）。规则：
- 因本机数据/环境缺失而失败或 error 的测试：如实记录（测试名 + 失败原因），**不修改测试来硬绿**；
- 因合并引入的代码错误而失败的测试：属于合流缺陷，必须修复（修改的是合流产物，不是测试）；
- 若出现 front3 测试依赖 pytest 插件缺失等环境问题：记录并报告，不得擅自装包。

- [ ] **Step 6: 真实数据集成 A/B 回归**

```powershell
E:\manga translator agent\amta\.venv\Scripts\python.exe integration_ab_test.py
```

预期：3 项 PASS（主检出 `workspace/touhou-single-wing` 数据存在）。记录每项输出摘要。

- [ ] **Step 7: pyright 对比（可选但记录）**

```powershell
cd "E:\manga translator agent\amta"; uv run pyright 2>&1 | Select-Object -Last 5
```

记录错误数。若明显多于合并前（可先在 BASE 上跑一次 baseline 对比），定位是新合流代码引入的才需处理，front3 遗留的如实记录。

- [ ] **Step 8: 写报告**

报告写到 `E:\manga translator agent\amta\.superpowers\sdd\2026-08-29-lesson03-p0p1\task-1-report.md`：合并结果、冲突文件清单与每处解决方式、pytest 总数与结果（含跳过/失败明细）、integration A/B 输出摘要、pyright 前后对比、遗留风险。

---

### Task 2: get_context 缺口单元测试 + 集成 A/B 测试收编

**Files:**
- Modify: `tests/test_context_semantic_transfer.py`（追加 5 个测试）
- Modify: `src/amta/translate_tools.py:~169`（仅一处：页头计数的 TDD 修复）
- Create: `tests/test_get_context_ab.py`
- Delete: `integration_ab_test.py`（根目录脚本）

**Interfaces:**
- Consumes: Task 1 合流后的 `translate_tools.execute_tool("get_context", {"pages": int}, work_state, state_dir=...)` → str；常量 `MAX_REGIONS_PER_PAGE=15`、`MAX_TERMS=5`；测试文件内既有 helper `_write_page_artifacts`。
- Produces: 合计 10 个 get_context 单元测试 + 1 个真实数据 A/B pytest 用例；`integration_ab_test.py` 不再存在。

**测试性质说明（Ruling）:** 下列测试 1/3/4/5 是**行为锁定测试**（characterization tests）——针对已实现行为补覆盖，预期直接绿；若任何一条红了，说明发现真实 bug：停下、把失败原文写进报告、不得改测试来迁就错误行为。只有测试 2 是真 TDD（先红后绿 + 一行代码修复）。

- [ ] **Step 1: TDD——页头计数与渲染条数一致（先红后绿）**

追加测试（`tests/test_context_semantic_transfer.py` 文件末尾）：

```python
def test_get_context_page_header_count_matches_rendered(tmp_path):
    """TDD：页头「共N条」应等于实际渲染条数（截断时=15），而不是页内总数。"""
    from amta import translate_tools

    state_dir = tmp_path / "state"
    state_dir.mkdir()
    artifacts_dir = tmp_path / "artifacts"
    artifacts_dir.mkdir()

    canon = [
        {"region_id": f"page_1_u{i:02d}", "text": f"原文{i}", "page": 1, "category": "dialogue_bubble"}
        for i in range(16)
    ]
    translations = {f"page_1_u{i:02d}": f"译文{i}" for i in range(16)}
    _write_page_artifacts(artifacts_dir, page=1, canon=canon, translations=translations)

    out = translate_tools.execute_tool("get_context", {"pages": 1}, {}, state_dir=state_dir)

    header = next(line for line in out.splitlines() if line.startswith("--- 第1页"))
    assert "共15条" in header
    assert "共16条" not in header
```

运行并确认 RED：`.venv\Scripts\python.exe -m pytest tests\test_context_semantic_transfer.py::test_get_context_page_header_count_matches_rendered -v`（当前实现页头写 `共{len(regions)}条` → 16 条时断言失败）。

最小修复（`src/amta/translate_tools.py` 的 `_build_semantic_context` 内）：

```python
# 原：parts.append(f"--- 第{page_num}页（共{len(regions)}条）---")
parts.append(f"--- 第{page_num}页（共{min(len(regions), MAX_REGIONS_PER_PAGE)}条）---")
```

运行确认 GREEN，然后提交：

```powershell
git -C "E:\manga translator agent\amta" add tests/test_context_semantic_transfer.py src/amta/translate_tools.py
git -C "E:\manga translator agent\amta" commit -m "fix: get_context 页头计数改为渲染条数（TDD，截断时不再虚报总数）"
```

- [ ] **Step 2: 行为锁定测试 ×4（预期直接绿；红=发现真 bug，停下报告）**

```python
def test_get_context_truncates_regions_to_fifteen(tmp_path):
    """边界：单页 region 数超过 MAX_REGIONS_PER_PAGE(15) 时只渲染前 15 条。"""
    from amta import translate_tools

    state_dir = tmp_path / "state"
    state_dir.mkdir()
    artifacts_dir = tmp_path / "artifacts"
    artifacts_dir.mkdir()

    canon = [
        {"region_id": f"page_1_u{i:02d}", "text": f"原文{i}", "page": 1, "category": "dialogue_bubble"}
        for i in range(16)
    ]
    translations = {f"page_1_u{i:02d}": f"译文{i}" for i in range(16)}
    _write_page_artifacts(artifacts_dir, page=1, canon=canon, translations=translations)

    out = translate_tools.execute_tool("get_context", {"pages": 1}, {}, state_dir=state_dir)

    rendered = [line for line in out.splitlines() if line.startswith("[对话]")]
    assert len(rendered) == 15
    assert "译文0" in out
    assert "译文14" in out
    assert "译文15" not in out  # 第 16 条被截断


def test_get_context_caps_terms_at_five(tmp_path):
    """边界：相关 confirmed 术语超过 MAX_TERMS(5) 时只显示前 5 条（按插入序）。"""
    from amta import translate_tools

    state_dir = tmp_path / "state"
    state_dir.mkdir()
    artifacts_dir = tmp_path / "artifacts"
    artifacts_dir.mkdir()

    combined_src = "八意様 月の民 蓬莱 山の幸 玉鱗 ドカン"
    _write_page_artifacts(
        artifacts_dir, page=1,
        canon=[{"region_id": "page_1_u00", "text": combined_src, "page": 1, "category": "dialogue_bubble"}],
        translations={"page_1_u00": "译文占位"},
    )
    ws = {
        "terms": {
            "八意様": {"translation": "译一", "status": "confirmed", "source": "p1"},
            "月の民": {"translation": "译二", "status": "confirmed", "source": "p1"},
            "蓬莱": {"translation": "译三", "status": "confirmed", "source": "p1"},
            "山の幸": {"translation": "译四", "status": "confirmed", "source": "p1"},
            "玉鱗": {"translation": "译五", "status": "confirmed", "source": "p1"},
            "ドカン": {"translation": "译六", "status": "confirmed", "source": "p1"},  # 第 6 条，应被截断
        }
    }

    out = translate_tools.execute_tool("get_context", {"pages": 1}, ws, state_dir=state_dir)

    assert "译一" in out and "译五" in out
    assert "译六" not in out


def test_get_context_multipage_reads_last_two_pages(tmp_path):
    """pages=2 时读取页码最大的 2 页（translation 文件按页码排序取尾部）。"""
    from amta import translate_tools

    state_dir = tmp_path / "state"
    state_dir.mkdir()
    artifacts_dir = tmp_path / "artifacts"
    artifacts_dir.mkdir()

    for page, text in [(1, "甲页内容"), (2, "乙页内容"), (3, "丙页内容")]:
        _write_page_artifacts(
            artifacts_dir, page=page,
            canon=[{"region_id": f"page_{page}_u00", "text": f"原文{page}", "page": page, "category": "dialogue_bubble"}],
            translations={f"page_{page}_u00": text},
        )

    out = translate_tools.execute_tool("get_context", {"pages": 2}, {}, state_dir=state_dir)

    assert "--- 第2页" in out
    assert "--- 第3页" in out
    assert "乙页内容" in out
    assert "丙页内容" in out
    assert "甲页内容" not in out


def test_get_context_tolerates_malformed_canon(tmp_path):
    """容错：canon 含非 dict 项 / 缺 region_id 项 / 坏 JSON 时不得崩溃，仍输出有效内容。"""
    from amta import translate_tools

    state_dir = tmp_path / "state"
    state_dir.mkdir()
    artifacts_dir = tmp_path / "artifacts"
    artifacts_dir.mkdir()

    canon = [
        "不是字典",
        {"no_region_id": True},
        {"region_id": "page_1_u00", "text": "こんにちは", "page": 1, "category": "dialogue_bubble"},
    ]
    (artifacts_dir / "page_1_canon.json").write_text(json.dumps(canon, ensure_ascii=False), encoding="utf-8")
    (artifacts_dir / "page_1_translation.json").write_text(
        json.dumps({"work_id": "test", "translations": {"page_1_u00": "你好"}}, ensure_ascii=False),
        encoding="utf-8",
    )
    # 另一页 canon 是坏 JSON：该页退化为无 category 标注（label=文本），但不崩溃
    (artifacts_dir / "page_2_canon.json").write_text("{broken json", encoding="utf-8")
    (artifacts_dir / "page_2_translation.json").write_text(
        json.dumps({"work_id": "test", "translations": {"page_2_u00": "第二页"}}, ensure_ascii=False),
        encoding="utf-8",
    )

    out = translate_tools.execute_tool("get_context", {"pages": 2}, {}, state_dir=state_dir)

    assert "你好" in out
    assert "第二页" in out
    assert "[文本]" in out  # 坏 canon 页的 label 回退
```

运行：`.venv\Scripts\python.exe -m pytest tests\test_context_semantic_transfer.py -v` → 全绿（9 个）。任何红：停下写报告，不得改测试迁就。

- [ ] **Step 3: 提交**

```powershell
git -C "E:\manga translator agent\amta" add tests/test_context_semantic_transfer.py
git -C "E:\manga translator agent\amta" commit -m "test: 补 get_context 边界覆盖（15截断/术语上限/多页/坏canon容错）"
```

- [ ] **Step 4: 集成 A/B 测试收编进 pytest**

创建 `tests/test_get_context_ab.py`（先读根目录 `integration_ab_test.py` 的 test 3 全文，忠实移植其逻辑后按下面骨架 pytest 化；原 #1/#2 与 tmp_path 单元测试同构，不迁移、随脚本一并删除）：

```python
"""Lesson 03 集成 A/B 测试（pytest 收编版）。

原根目录脚本 integration_ab_test.py 的三个用例去向：
- #1 结构化读取 / #2 回退兼容：与 tests/test_context_semantic_transfer.py 的
  tmp_path 单元测试同构，收编时删除（避免双份维护）；
- #3 真实数据 A/B 对比：迁移到本文件，数据缺失时显式 SKIP（不再静默）。
"""
import json
import sys
from pathlib import Path

import pytest

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "src"))

REPO_ROOT = Path(__file__).resolve().parents[1]
STATE_DIR = REPO_ROOT / "workspace" / "touhou-single-wing" / "state"


@pytest.mark.skipif(not STATE_DIR.exists(), reason="真实项目数据仅存在于本机 workspace/（gitignored）")
def test_ab_comparison_real_data():
    """真实数据 A/B：新实现（结构化）对比旧实现（裸读汇总 translation.json）。"""
    from amta import translate_tools

    work_state = json.loads((STATE_DIR / "work_state.json").read_text(encoding="utf-8"))
    new_out = translate_tools.execute_tool("get_context", {"pages": 1}, work_state, state_dir=STATE_DIR)
    assert new_out.strip(), "新实现返回为空"
    assert "[对话]" in new_out  # 真实数据 p19 全为对话气泡（Lesson 03 报告结论）

    agg_path = STATE_DIR.parent / "artifacts" / "translation.json"
    assert agg_path.exists(), "汇总 translation.json 不存在"
    doc = json.loads(agg_path.read_text(encoding="utf-8"))
    trans = doc.get("translations", {})
    by_page = {}
    for rid in trans:
        parts = rid.split("_")
        if len(parts) >= 2 and parts[1].isdigit():
            by_page.setdefault(parts[1], []).append(rid)
    last_page = max(by_page, key=int)
    old_lines = [f"[{rid}] {trans[rid]}" for rid in sorted(by_page[last_page])]
    assert old_lines, "旧实现口径下最后一页无译文"
```

（骨架基于原脚本逻辑改写；若原 test 3 有额外断言且语义仍成立，一并保留。）

删除脚本并提交：

```powershell
git -C "E:\manga translator agent\amta" rm integration_ab_test.py
git -C "E:\manga translator agent\amta" add tests/test_get_context_ab.py
git -C "E:\manga translator agent\amta" commit -m "refactor: 集成A/B测试收编进 pytest（真实数据缺失显式SKIP，删除根目录脚本）"
```

- [ ] **Step 5: 全量验证 + 报告**

```powershell
E:\manga translator agent\amta\.venv\Scripts\python.exe -m pytest tests\ -q
```

预期全绿（其中真实数据 A/B 在本机应 PASS 而非 SKIP）。报告写 `task-2-report.md`：TDD 证据（RED 输出原文 + GREEN 输出）、新增测试清单、全量测试数、收编决策说明。

---

### Task 3: Stage 2-d Slice 2——CLAUDE.md 记忆库触发行 + 写回约定（feat/project-memory worktree）

**Files:**
- Modify: `E:\manga translator agent\amta\.worktrees\feat-project-memory\CLAUDE.md`（已确认存在于 feat/project-memory @ db8c2d3）

**Interfaces:**
- Consumes: `scripts/memory.py`（同 worktree 已存在，CLI 子命令 search/add/read/stats）；`docs/INDEX.md`（Slice 1 已建，37 条）。
- Produces: CLAUDE.md 新增"项目记忆库使用约定"小节；feat/project-memory 分支新 commit。不合并进其他分支。

**规格（来自 STATE.md 待办 #9）:** 触发行 = "改翻译策略/工位架构/选型前先 `python scripts/memory.py search <关键词>`"；写回约定 = 迭代结束产出可复用经验（lesson/ADR 级）时，用 `python scripts/memory.py add` 写回记忆库。

- [ ] **Step 1: 定位与冒烟**

```powershell
git -C "E:\manga translator agent\amta" worktree list   # 确认 .worktrees\feat-project-memory @ feat/project-memory
git -C "E:\manga translator agent\amta\.worktrees\feat-project-memory" status --short   # 必须为空
git -C "E:\manga translator agent\amta\.worktrees\feat-project-memory" rev-parse HEAD   # 记录 BASE（应为 db8c2d3）
```

在该 worktree 目录用任一可用 Python 冒烟（memory.py 零依赖）：

```powershell
E:\manga translator agent\amta\.worktrees\feat-project-memory\.venv\Scripts\python.exe scripts\memory.py search 拟声
```

（若该 worktree 无 .venv，用主检出 venv 的 python.exe 但工作目录必须在 worktree 内。）确认命令真实可跑、有输出后，把**验证过的确切命令**写进触发行——命令与规格不符时以实测为准（Ruling：可运行的真命令 > 文档里的理想命令）。

- [ ] **Step 2: 编辑 CLAUDE.md**

先读全文，在合适位置（遵循其现有结构）追加小节，内容要点（措辞按仓库既有风格调整）：

```markdown
## 项目记忆库使用约定（Stage 2-d Slice 2）

**检索触发（改之前先查）：** 凡要改动翻译策略、工具/工位架构、技术选型，先执行：
`<实测验证的命令> search <关键词>`
（例：改 SFX 处理前 search 拟声）。命中索引条目先 `read` 原文再动手，避免重蹈已记录的坑。

**写回约定（干完就沉淀）：** 迭代结束若产出可复用经验（等价 lesson/ADR 级别的决策、踩坑、口径），执行：
`<实测验证的命令> add`
写回记忆库；INDEX 条目的一句话价值由本agent校准，不留空。
```

- [ ] **Step 3: 提交**

```powershell
git -C "E:\manga translator agent\amta\.worktrees\feat-project-memory" add CLAUDE.md
git -C "E:\manga translator agent\amta\.worktrees\feat-project-memory" commit -m "docs: Stage 2-d Slice 2 - CLAUDE.md 记忆库触发行 + 写回约定"
```

- [ ] **Step 4: 报告**

报告写 `task-3-report.md`：冒烟命令与输出摘要、CLAUDE.md 追加段全文、commit SHA。**不合并**该分支（保持支线，等 Lesson 05 统一收编——Ruling，成本：Slice 2 的成果暂不在主工作线生效，如果错了只需 revert 一个 docs commit）。

---

### Task 4: 学习路径文档落盘（主线执行，不派 implementer）

**Files:**
- Modify: `E:\Agent Loop Learning\STATE.md`（第十二次更新）
- Create: `E:\Agent Loop Learning\learning-records\0014-lesson03-p0p1-merge-and-slice2.md`

由控制者（主线）在全部任务审稿通过后执行：按仓库既有格式更新 STATE.md（合流拓扑、测试数、Slice 2 完成、检验题已抛出、下一步 Lesson 04）、写 learning-record 0014、commit 到该仓库 master（该仓库惯例即直提 master）。

---

### Task 5: 终审 + 交付（主线执行）

按 subagent-driven-development 的 Final Review：全计划范围 diff（BASE=92438f2 → HEAD）交最终审稿 subagent；通过后主线生成交付报告（HTML），并向 Patrick 抛出：Lesson 03 检验题原文、INDEX 校准作业、以及 finishing-a-development-branch 的整合选项（feature 线是否并入 main / 是否 push）。
